# Variables d'environnement locales — NE PAS COMMITER (déjà dans .gitignore).
# Modèle de référence : .env.example
# Les vraies variables d'environnement gardent la priorité sur ce fichier.

# ── Base de données ──────────────────────────────────────────────
DB_URL=jdbc:postgresql://localhost:5432/stock_db
DB_USERNAME=postgres
DB_PASSWORD=1234

# ── JWT ──────────────────────────────────────────────────────────
# Clé fraîche générée le 2026-09-18 (l'ancienne était committée, donc compromise).
# Les tokens émis avec l'ancienne clé sont invalidés.
JWT_SECRET=K0i+73WaTT8QnofAyuylmSsBU2bZd2jZ1i5npVWdvFkNbjk2XKtID0AvM76S5k20
JWT_EXPIRATION_MS=86400000

# ── Admin de bootstrap (premier démarrage, base vide) ────────────
ADMIN_LOGIN=admin@sgs.local
ADMIN_PASSWORD=admin123
ADMIN_ROLE=ADMIN

# ── CORS ─────────────────────────────────────────────────────────
CORS_ALLOWED_ORIGINS=http://localhost:4200,http://localhost:5173

# ── Rate limiting du login (optionnel, défauts : 5 / 300) ────────
#RATE_LIMIT_LOGIN_MAX_FAILURES=5
#RATE_LIMIT_LOGIN_WINDOW_SECONDS=300
